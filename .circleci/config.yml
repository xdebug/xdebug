version: 2.1
commands:
  compile:
    description: Compiles and sets-up common environment vars
    steps:
      - checkout
      - run: ./.build.scripts/compile.sh
      - run:
          name: Setup env vars
          command: |
            echo 'export CWD=`eval echo "${CIRCLE_WORKING_DIRECTORY}"`' >> $BASH_ENV
  compile-no-zlib:
    description: Compiles and sets-up common environment vars
    steps:
      - checkout
      - run: ./.build.scripts/compile.sh --without-xdebug-compression
      - run:
          name: Setup env vars
          command: |
            echo 'export CWD=`eval echo "${CIRCLE_WORKING_DIRECTORY}"`' >> $BASH_ENV
  run-tests:
    description: Runs the tests
    steps:
      - run:
          name: Run tests with env vars
          command: php run-xdebug-tests.php -j8 -q -x --show-diff

jobs:
  "PHP 8 5 without opcache":
    docker:
      - image: cimg/php:8.5.4
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 5 with opcache":
    docker:
      - image: cimg/php:8.5.4
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d opcache.enable=1 -d opcache.enable_cli=1 -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 4 without opcache":
    docker:
      - image: cimg/php:8.4.19
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 4 with opcache":
    docker:
      - image: cimg/php:8.4.19
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=opcache.so -d opcache.enable=1 -d opcache.enable_cli=1 -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 3 without opcache":
    docker:
      - image: cimg/php:8.3.30
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 3 with opcache":
    docker:
      - image: cimg/php:8.3.30
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=opcache.so -d opcache.enable=1 -d opcache.enable_cli=1 -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 2 without opcache":
    docker:
      - image: cimg/php:8.2.30
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile-no-zlib
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 2 with opcache":
    docker:
      - image: cimg/php:8.2.30
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=opcache.so -d opcache.enable=1 -d opcache.enable_cli=1 -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 1 without opcache":
    docker:
      - image: cimg/php:8.1.34
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

  "PHP 8 1 with opcache":
    docker:
      - image: cimg/php:8.1.34
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PW
    steps:
      - checkout
      - compile
      - run:
          name: Setup extra env vars
          command: |
            echo 'export TEST_PHP_ARGS="-n -d foo=yes -d zend_extension=opcache.so -d opcache.enable=1 -d opcache.enable_cli=1 -d zend_extension=${CWD}/modules/xdebug.so"' >> $BASH_ENV
      - run-tests
    environment:
      REPORT_EXIT_STATUS: 1

workflows:
  version: 2
  testing:
    jobs:
      - "PHP 8 5 without opcache"
      - "PHP 8 5 with opcache"
      - "PHP 8 4 without opcache"
      - "PHP 8 4 with opcache"
      - "PHP 8 3 without opcache"
      - "PHP 8 3 with opcache"
      - "PHP 8 2 without opcache"
      - "PHP 8 2 with opcache"
      - "PHP 8 1 without opcache"
      - "PHP 8 1 with opcache"
